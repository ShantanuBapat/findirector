# Copy to .env and fill in real values (.env is gitignored).
# These configure the local pgvector database (docker-compose.yml) and the
# Python code that connects to it.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=findirector
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
